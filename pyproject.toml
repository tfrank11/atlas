[project]
name = "atlas"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "tfrank11", email = "timmyfrank3@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "cloudpickle>=3.1.2",
    "msgpack>=1.2.1",
]

[project.scripts]
client = "atlas.client:main"
scheduler = "atlas.scheduler:main"
worker = "atlas.worker:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
